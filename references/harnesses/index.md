# Harness engineering wiki-book -- index

Maintained by `airchon` (`.apm/skills/airchon/SKILL.md`).
Prose-navigable reference on how AI agent harnesses actually work --
Claude Code and GitHub Copilot CLI specifically, agent-harness
engineering generally. Every claim in every page is tagged VERIFIED
(fetched from a named, authoritative source this session or a prior
one) or BEST CURRENT UNDERSTANDING, UNCONFIRMED -- never blended. See
each page's own "Sources" section for what was actually checked and
when.

Written LAZY, on demand, as real questions need each topic -- not
pre-built speculatively. A missing page below is real follow-up work,
not an oversight.

## Reader proficiency tiers

**Relocated 2026-09-03.** The reader-proficiency-tiers rubric no longer
lives in this wiki-book -- it moved to
`resources/airchon-teacher/reader-proficiency-tiers.md`, the same
relocation reasoning already applied to the session-pacing agenda files
under Curriculum below (see that section and `CHANGELOG.md`): it is a
meta/rubric page authored by the operator, classifying readers into
tiers, not a `references/harnesses/` research page about harness
internals, and belongs in `airchon-teacher`'s own tier domain instead.
Read it directly at its new path for the current four-tier rubric
(Slumberer -> Gnostic -> Demiurge -> Archon) and reading lists -- the
21-page core-mechanics band plus the RAG, Agentic-SDLC, AI-model
-classification, and local-inference-engine practitioner-mechanics
clusters for Gnostic->Demiurge, and the 10-page design-space band plus
the Agentic-SDLC design-space/methodology cluster for Demiurge->Archon
(the last two clusters, covering `references/models/` and
`references/inference-engines/`, were added the same day this page
relocated). `knowledge-path-curriculum.md` -- relocated out of this
wiki-book that same day too, see the Curriculum section below -- still
expands those reading lists into a teachable structure and links back
to this page from its own new, adjacent location.

## Curriculum

**Relocated 2026-09-03.** The knowledge-path curriculum scaffold no
longer lives in this wiki-book -- it moved to
`resources/airchon-teacher/knowledge-path-curriculum.md`, the same day
and for the same reason the reader-proficiency-tiers rubric it expands
relocated to that directory (see the Reader proficiency tiers section
above): it is a curriculum scaffold authored by the operator, not a
`references/harnesses/` research page about harness internals, and
belongs in `airchon-teacher`'s own tier domain instead, alongside the
rubric it expands and the session-pacing agenda files
(`*-sessions.md`) that already lived there. Read it directly at its new
path for the full three-transition course structure -- per-transition
learning objectives, a per-source-page module (key concepts +
comprehension check + exercise) for every page named in the
reader-proficiency-tiers reading lists, and a capstone synthesis task
per transition, including the Cluster 6-10 extensions covering
`references/rag/`, `references/sdlc/`, `references/models/`, and
`references/inference-engines/`. Every factual detail it cites about
what a source page covers remains inherited from that page's own row
below (or from `references/rag/index.md` / `references/sdlc/index.md` /
`references/models/index.md` / `references/inference-engines/index.md`
for Clusters 6-10), not re-verified on the curriculum page itself --
that inheritance discipline did not change with the move.

## Topics

Reading order note: [agent-topology.md](agent-topology.md) is written
to precede [agent-loop.md](agent-loop.md) -- it is listed first in the
table below for that reason, not by write date. Every other row is
still in write-date order.

Coverage note (2026-09-01): pi (`@earendil-works/pi-coding-agent`, part
of the `earendil-works/pi` monorepo alongside sibling packages `pi-ai`,
`pi-tui`, `pi-agent-core`, and a private `pi-evals`) reached full 29/29
per-harness-page parity with Claude Code, Copilot CLI, and OpenCode
earlier this same session, following a batch of 20 sibling additions
(see each affected row below for its own pi annotation) that brought it
from 9/29 to 29/29. Hermes Agent (Nous Research,
`github.com/NousResearch/hermes-agent`) has now reached that same full
29/29 parity later the same day, via a second batch of 22 sibling
additions across every page that previously lacked a dedicated Hermes
Agent section (see each affected row below for its own Hermes Agent
annotation) that brought it from 7/29 to 29/29 -- sourced, where the
harness's own public, MIT-licensed repository allows it, directly from
implementation source rather than only from its hosted docs site (see
each section's own sourcing note for which standard applies there).
DeepSeek Harness remains partial -- its per-page count is unchanged by
either batch and is separate, not-yet-scheduled follow-up work.

| Topic | Status |
|---|---|
| [Middleware-composed general-purpose agent harnesses: LangChain's Deep Agents and the library-as-harness design space](middleware-composed-agent-harnesses.md) | Written 2026-08-24 -- a **third** category this book had not named before, verified live rather than assumed from the request framing: not a finished terminal product (Claude Code/Copilot CLI/OpenCode) and not a fixed-graph orchestrator ([deterministic-orchestration.md](deterministic-orchestration.md)'s LangGraph/Conductor subjects), but a **dynamically-looping, general-purpose agent harness distributed as a composable library**. Confirms and sharpens the requester's own framing into a source-verified three-layer stack (`github.com/langchain-ai/deepagents`'s own `libs/ARCHITECTURE.md`, fetched this session): LangGraph (runtime: state/checkpoints/streaming/interrupts) underneath LangChain's own bare, capability-free `create_agent()` underneath Deep Agents' `create_deep_agent()` (an opinionated, bundled middleware/backend/subagent/skills/memory/profile default stack) -- with a `CompiledSubAgent` mechanism as a directly sourced, tool-call-level bridge back into the deterministic-orchestrator category (any compiled LangGraph `StateGraph` can be handed in as a subagent). Documents **middleware** as a general concept distinct from this book's own event-dispatch hook vocabulary ([hooks-lifecycle-extensibility.md](hooks-lifecycle-extensibility.md)) -- an in-process, construction-time-composed onion wrapper around the model call/tool execution/state prep, able to rewrite the request *before* the step it wraps runs, not merely observe or veto it afterward; a fully source-read (`graph.py`, `THREAT_MODEL.md`, the `middleware/` directory listing) default middleware stack and ordering, including an honestly-flagged, precisely-sourced discrepancy between two first-party docs pages and the SDK's own source tree over whether "planning" (a `write_todos` tool) ships bundled by default at all; the `BackendProtocol` virtual-filesystem abstraction and its six pluggable storage strategies (`StateBackend` default/thread-scoped, `FilesystemBackend`/`LocalShellBackend` real-disk-plus-shell with a named, currently-deprecated `virtual_mode` insecure default, `StoreBackend`/`ContextHubBackend` cross-thread, sandboxed remote backends, and `CompositeBackend` path-prefix routing); the `task()` subagent-delegation tool alongside `CompiledSubAgent` and a genuinely new async/remote-server delegation shape (`AsyncSubAgentMiddleware`); a `SKILL.md` progressive-disclosure skills system explicitly targeting the named, shared `agentskills.io` cross-vendor specification rather than only converging by observation with Claude Code's/Copilot CLI's own conventions; `AGENTS.md` as writable, namespace-scoped semantic memory; model/provider-keyed `HarnessProfile` overrides as a public, caller-writable analogue of Claude Code's own internal model-conditional tool-withholding; `FilesystemPermission` allow/deny/interrupt rules bridging into LangGraph's own `interrupt()`; a `DeltaChannel`-backed `DeepAgentState` for linear checkpoint growth; and a fully source-read, dated, machine-generated `THREAT_MODEL.md` stating an explicit "trust the LLM" security posture with no bundled OS-level sandbox -- the same no-OS-sandbox finding this book had previously reached only for OpenCode, now reached a third time for a third structurally different harness. Closes with a cross-instance synthesis table verdict-tagging each mechanism against this book's existing per-CLI and deterministic-orchestration.md coverage, and an explicitly out-of-scope flag on "Deep Agents Code," the same organization's own terminal-CLI product built on top of this SDK, named as real follow-up work rather than covered here |
| [Deterministic orchestration: LangGraph, Microsoft Conductor, and the graph/workflow-as-harness design space](deterministic-orchestration.md) | Written 2026-08-24, generalized same day (renamed from its original LangGraph-only title/file, `deterministic-orchestration-and-langgraph.md`) once a second, independently-built instance of the same category surfaced, then extended again the same day with a **third** instance -- **GitHub Agentic Workflows** (`github.com/github/gh-aw`, official docs), added after actually reading `danielmeppiel.github.io/agentic-sdlc-handbook` end to end and ruling the handbook itself out of scope as a primary source (it is an organisational-methodology/process guide, not harness-internals documentation -- see the page's own Sources section for the full ruling) while pursuing the one narrower, independently-re-verified mechanism its ch.16 pointed at: new §14 documents GitHub Agentic Workflows' Markdown-plus-frontmatter-compiled-to-`.lock.yml` authoring surface (a fourth position on §1.1's code-vs-declarative axis -- source and executed artifact are different files) and its `engine:` property, which installs and runs a workflow step through the *real* vendor CLI (Copilot, Claude Code, Codex, Gemini, or this book's own **pi**) rather than a bare model API, doubly-sourcing §8.1's Conductor-derived "orchestrator wraps a complete other harness" finding and adding pi as a sixth wrapped harness; new §15 documents **safe outputs** as this page's other genuinely new general concept -- a read-only agent job handing structured intent to a wholly separate, write-scoped GitHub Actions job via two-stage pre-check/apply-time validation, a job/credential-boundary capability separation for the deterministic/probabilistic boundary that this page argues is structurally stronger than any in-process permission gate found anywhere else in this book, explicitly distinguished from Conductor's own fail-open, same-run `validator` block; new §16 is a three-way synthesis table. The book's first page on a genuinely different *category* of harness: not an interactive coding-assistant CLI like Claude Code/Copilot CLI/OpenCode, but a **deterministic orchestrator**, documented here as three sourced instances side by side. **LangGraph** (`github.com/langchain-ai/langgraph`) -- a developer-facing Python/JS library, control flow authored in code: names the "workflows have predetermined code paths... agents are dynamic and define their own processes" author-control-vs-runtime-freedom axis, its Pregel-inspired Bulk-Synchronous-Parallel superstep execution model, the Graph API (`StateGraph`, `Command`, `Send`, `.compile()`), the Functional API (`@entrypoint`/`@task`, held to slightly lower confidence), checkpointer-backed persistence (`thread_id`/`checkpoint_ns`/`checkpoint_id`, time travel, non-destructive forking via `update_state()`), `interrupt()`/`GraphInterrupt`/`Command(resume=)` human-in-the-loop, per-node `RetryPolicy`/`NodeTimeoutError`, and the network/supervisor/hierarchical/swarm multi-agent patterns (`create_supervisor`/`create_handoff_tool`/`create_swarm`, swarm's persisted `active_agent` state flagged as the first genuinely hub-free peer topology this book has sourced anywhere). **Microsoft Conductor** (`github.com/microsoft/conductor`, verified this session via `gh` as a real, distinct, actively-maintained project -- explicitly *not* Netflix's unrelated older Conductor engine) -- a CLI tool defining workflows in plain YAML with no code authored at all, "no LLM in the orchestration loop": its eight-step-type schema and offline `conductor validate` static pass; a genuinely new general concept -- an orchestrator sitting one layer *above* another harness this book documents (its `claude-agent-sdk`/`copilot` providers literally wrap Claude Code's own Agent SDK and the Copilot CLI's own SDK as a single workflow step, not a bare model API); four distinct human-in-the-loop primitives (`human_gate`, the multi-question navigable `questions` step, LLM-judged `dialog` mode, and a shipped Reflexion-style `validator` grade-and-single-retry block filling a gap advanced-planning-and-execution-architectures.md had flagged as unshipped in all three CLIs) plus a fifth, no-pause-required `conductor guide` steering channel; `audit`-vs-`enforce` staged budget enforcement; and two more new general concepts -- fleet-level supervision of many concurrently running instances as a first-class open-source CLI feature (run records, an escalating stop ladder with PID-reuse identity guards, the Fleet Manager Textual TUI), and a workflow-definition-as-a-versioned-registry-distributed-artifact model (`@registry#ref` syntax, pinned-SHA-vs-floating-ref semantics) -- neither of which LangGraph's own documentation names an equivalent for. Closes with two synthesis tables: LangGraph against this book's existing per-CLI pages (unchanged from the original write), and a new cross-instance table verdict-tagging each Conductor mechanic as either "same concept, renamed" (conditional routing, per-node retry, sub-pipeline composition) or genuinely new (the three items above, plus most of the human-in-the-loop taxonomy) |
| [Observability and self-diagnostics](observability-and-self-diagnostics.md) | Written 2026-08-17, updated 2026-09-01 (new §4 pi: a real internal telemetry span schema, `@earendil-works/pi-telemetry`, but deliberately no exporter/OTel dependency shipped -- the choice of backend left entirely to the caller; renumbered Synthesis to §5, Sources to §6), updated again 2026-09-01 (new §5 Hermes Agent + renumbered Synthesis to §6, Sources to §7: the most extensively documented observability surface of any harness on this page -- a plugin-populated `hermes doctor` health-check registry rather than a fixed checklist, a supply-chain package-poisoning advisory scanner, five separate log files behind a filtering DSL, and a bundled fail-open third-party tracing plugin standing in for any built-in OTLP exporter) -- the final item on the gap-analysis list (`HANDOFF-missing-topics.md`, now deleted, its "two thinner gaps" section fully resolved): debugging/operating a harness's own execution, distinct from [auth-and-usage-accounting.md](auth-and-usage-accounting.md) §1.4's cost-and-usage-only OTel metrics/events catalogue, cross-referenced rather than repeated. Claude Code's docs-verified debug-flag family (`--debug`/`--debug=category`/`--debug-file`, `/debug`, `--safe-mode`, `CLAUDE_CONFIG_DIR`-isolation, `/heapdump`), its introspection-command family (`/context`/`/hooks`/`/mcp`/`/status`) alongside `/doctor`/`claude doctor` (cross-referenced to built-in-skills.md, not repeated), a fully documented separate-and-beta OpenTelemetry **traces** signal (`CLAUDE_CODE_ENHANCED_TELEMETRY_BETA`, the `claude_code.interaction`/`llm_request`/`tool`/`tool.execution`/`hook` span hierarchy, W3C `traceparent` propagation into subprocesses and from an embedding caller), and a data-usage-docs-verified third layer -- Claude Code's own default-on vendor telemetry to Anthropic (latency/reliability metrics, redacted crash reports), independent of any customer-configured OTel pipe; Copilot CLI's changelog-verified logging surface (`log_level` config key, `--print-debug-info`, `--log-dir`) and the session's central corrective finding -- its OpenTelemetry export is not cost-metric-only the way caching.md's prior citation implied, but includes genuine execution-structure spans (subagent `INTERNAL`-kind spans, GenAI-convention `tool_call` spans, a hook-span-to-span-event demotion) now also reachable via a July-2026 enterprise-managed-settings `telemetry` block shared with VS Code; OpenCode's docs-verified `--log-level`/`--print-logs`/log-file-retention surface held apart from a source-checked negative finding (an open, unmerged OpenTelemetry integration PR, zero core-source hits) that pins its OTel story entirely on an unverified third-party plugin ecosystem, the same shape this book already found for OpenCode's native RAG support; closing with a from-scratch synthesis arguing observability is at least four separable layers (cost export, execution tracing, interactive debug surface, vendor product telemetry) that a harness should keep structurally distinct rather than sharing one undifferentiated pipe |
| [Packaging, distribution, and self-update mechanics](packaging-distribution-and-self-update.md) | Written 2026-08-17, updated 2026-09-01 (new §4 pi: npm is the PRIMARY distribution channel -- an outlier against the other three harnesses' curl/package-manager-first posture -- and the package was renamed from `@mariozechner/pi-coding-agent` to `@earendil-works/pi-coding-agent` after an acquisition Mario Zechner's own blog post titles "I've sold out"; renumbered synthesis to §5), updated again 2026-09-01 (new §5 Hermes Agent + renumbered Synthesis to §6: the one harness in this book's packaging coverage shipping no package-registry artifact for the CLI itself at all -- a `curl`/`irm`-piped installer performs a `git clone` of `NousResearch/hermes-agent` into a `uv`-managed virtual environment, the running artifact being source-plus-venv rather than a compiled binary or npm/PyPI package) -- the harness binary/package itself as the subject, closing the 8-item gap-analysis list started 2026-08-17 (see `HANDOFF-missing-topics.md` at the project root); explicitly "table stakes" coverage like tui-cli-application-architecture.md, not a differentiating design decision; corrects and supersedes retries.md's prior single-line auto-updater mention (a version-number error found and fixed this session: v2.1.147, not v2.1.144). Claude Code's docs-verified six-channel distribution (curl/`irm` native installer, two-channel Homebrew casks, WinGet, signed apt/dnf/apk repos, npm with per-platform `optionalDependencies` + postinstall, and the VS Code extension's own bundled-private-CLI-copy channel with its `autoInstallIdeExtension` reverse-auto-install mechanic), its manifest.json+GPG/platform-code-signing integrity chain, and a changelog-traced auto-update history (`autoUpdatesChannel`/`minimumVersion`, `DISABLE_AUTOUPDATER` vs. `DISABLE_UPDATES`, v2.1.207 streamed-download memory fix, v2.1.221 launcher-preservation fix, plugin-auto-update flagged as a distinct subsystem); Copilot CLI's README-verified four-channel distribution and changelog-traced dual native-binary/JS-fallback architecture (a likely Node.js SEA basis, named but not over-claimed), its `copilot update`/`/update`/`/upgrade`/team-only `/downgrade VERSION` command surface, a live GitHub Issue (#2615) and closed Discussion (#1199) corroborating `COPILOT_AUTO_UPDATE`/`--no-auto-update` and real-world version-pinning recovery, and a genuine two-reversal disk-cleanup-policy history; OpenCode's fully source-verified build pipeline (`packages/opencode/script/build.ts`'s 12-target baseline/musl build matrix, `publish.ts`'s `optionalDependencies`-map assembly mirroring both closed-source harnesses' independently-arrived-at npm shape, the CI code-signing/notarization detail, and adjacent Electron-desktop/VS-Code-extension build surfaces flagged but not analyzed) and its source-read `opencode upgrade`/`Installation.method()`/`upgrade()`/`latest()` per-install-method branching, with an explicit, honestly-flagged gap between the docs' "stays up to date automatically" claim and this session's inability to find the corresponding background-check logic in source |
| [TUI/CLI application architecture](tui-cli-application-architecture.md) | Written 2026-08-17, updated 2026-09-01 (new §4 pi: its TUI, `pi-tui`, is a wholly from-scratch, dependency-free rendering stack -- a fourth distinct architecture in this book alongside Ink, OpenTUI, and blessed; renumbered Synthesis to §5), updated again 2026-09-01 (new §5 Hermes Agent + renumbered Synthesis to §6: a sixth data point sourced from the GitHub API tree/contents endpoints rather than docs -- Hermes ships two independent, differently-implemented terminal front ends over one shared Python agent core, a base `prompt_toolkit` CLI and a from-scratch TypeScript/React TUI built on `@hermes/ink`, a private in-repo fork of Ink, a shape with no analogue elsewhere in this book) -- the rendering-engine/component-model/input-handling layer one level above [streaming-and-incremental-rendering.md](streaming-and-incremental-rendering.md)'s buffering/reassembly/pacing concerns (cross-referenced throughout, not duplicated); explicitly scoped as lower-priority "table stakes" coverage per the handoff that requested it. Claude Code's rendering engine reconstructed indirectly (Ink named as an adopter in Ink's own README, corroborated by an already-cross-referenced, explicitly-unadopted community reverse-engineering claim, plus the changelog's own Yoga-layout/cell-based-renderer facts) alongside a docs-verified two-path renderer (classic scrollback vs. alt-screen "fullscreen" mode) and a 19-context keybindings taxonomy standing in for an undocumented component tree; Copilot CLI's Ink usage directly confirmed via a first-party GitHub engineering blog post on its animated ASCII banner (JSX components, `useState`/`useEffect`, frame-timing engineering), its changelog-traced cycled-mode architecture (interactive/plan/autopilot, shell demoted out of the cycle in a later release, autopilot suppressing rather than bypassing the same dialog-invocation logic other modes use) and its independently-focusable Sessions sidebar; OpenCode's fully source-verified OpenTUI stack (a native Zig core over a C ABI, Flexbox layout, consumed via the Solid reconciler specifically), a three-tier primitive/composite/dialog component hierarchy read directly from its three-stage permission-dialog component, a genuine keybinding mode**-stack** (push/pop, shadowing) architecturally closer to a modal text editor than either closed-source harness's flatter scoping, a first-class leader-key-plus-which-key system with no confirmed equivalent elsewhere, and a newly-documented, genuinely remarkable cross-harness interop finding -- OpenCode's own external-editor integration directly reads and parses Claude Code's `~/.claude/ide/*.lock` file protocol to discover a connected IDE for editor-selection sync |
| [The multi-agent coordination design space](multi-agent-coordination-design-space.md) | Written 2026-08-17 -- a GENERAL-CONCEPTS page (no dedicated harness sections, same family as agent-loop.md/agent-topology.md), picking up exactly where [agent-topology.md](agent-topology.md) section 7 leaves its "fifth compositional question": surveys the general multi-agent-systems literature's own coordination patterns -- topology (centralized/decentralized/layered/shared-message-pool, grounded in arXiv:2402.01680's own taxonomy and MetaGPT's shared-message-pool worked example, arXiv:2308.00352), blackboard architectures (Hayes-Roth/Hearsay-II lineage via a 2025 restatement, arXiv:2510.01285), consensus/voting among peers (majority rule, LLM-Blender pairwise ranking, and debate, arXiv:2501.06322, cross-referenced against this book's own self-consistency/multi-agent-debate citations), and market-based task allocation (Reid Smith's 1980 Contract Net Protocol and its FIPA standardization) -- then places, without re-deriving, exactly where each of orchestration.md/fan-out.md/inter-agent-messaging.md's already-documented harness mechanics lands on that map: Claude Code's agent-team shared self-claimed task list as the book's clearest real blackboard instance (coexisting, in the same feature, with its addressed per-recipient mailbox -- offered as direct evidence a system can hold more than one point on the shared-scratchpad-vs-message-passing spectrum at once), `/deep-research`'s claim-level voting as the clearest real consensus instance (with Copilot CLI's identically-named but unrelated `vote_memory` tool flagged explicitly as a naming false friend, not peer consensus), and a consistent negative finding that none of the three harnesses implement genuine market-based/competitive-bidding task allocation, capability-based direct dispatch standing in its place everywhere checked |
| [Tool schema / interface design](tool-schema-and-interface-design.md) | Written 2026-08-17, updated 2026-09-01 (new §1.6/§2.5/§3.5/§4.6 GitHub Copilot CLI: an opaque-runtime/no-published-`input_schema` closed-source treatment grounded entirely in docs and hooks-reference fetches, closing this page's previously-noted Copilot CLI gap; separately updated 2026-09-01 with new pi material woven into §1.5/§2.4/§3.4/§4.5 plus Synthesis, rather than a dedicated pi section), separately updated 2026-09-01 with new Hermes Agent (Nous Research) material as four dedicated subsections, §1.7/§2.6/§3.6/§4.7 (an OpenAI-function-calling wire shape read directly from `tools/registry.py`'s `get_definitions()`; a three-way, mutually-reinforcing negative-boundary convention across an entire toolset -- e.g. the general-purpose `terminal` tool's own description steering the model by name toward `read_file`/`search_files`/`patch` instead, with each of those tools' own descriptions independently stating the same redirect back; the largest built-in surface this page documents, roughly 86 tools by Hermes' own reference page, consolidated unevenly via an `action` parameter alongside a three-tier, three-bridge-tool deferred-loading mechanism; and a fifth, independently-implemented nine-strategy fuzzy-match cascade plus a dedicated `_sanitize_tool_error()` layer scrubbing framing tokens out of raw exception text before it reaches the model) -- the design-space companion to [built-in-tools.md](built-in-tools.md)'s inventory: JSON Schema authoring grounded in Anthropic's own `define-tools`/`strict-tool-use` docs (the good-vs-poor description worked example, `strict: true` grammar-constrained sampling closing the type-coercion/missing-field failure modes, schema-attached `input_examples` explicitly distinguished from system-prompt-design-as-craft.md's prose few-shot examples); naming/description conventions grounded against the same Anthropic guidance (service-prefixed namespacing, explicit negative boundaries) and cross-checked against the Berkeley Function-Calling Leaderboard's own methodology pages (AST evaluation's function/parameter/type-matching breakdown, Function Relevance Detection as the closest independent instrument to what a poorly-named tool set breaks); the few-powerful-vs-many-narrow tradeoff grounded in Anthropic's own consolidation guidance and worked examples, then tested directly against this book's own prior tool-count inventory across all three harnesses -- surfacing OpenCode's consolidated `edit` permission key as real consolidation, Claude Code's `TodoWrite`->`Task*` split as a real move the *other* direction from Anthropic's own published guidance (with a BEST-CURRENT-UNDERSTANDING reconciliation offered, not asserted), and Copilot CLI's curated-default-GitHub-MCP-server tool list as a third real data point, plus deferred-schema-loading (`defer_loading`, Claude Code's `ToolSearch`) as a third strategy that shifts the tradeoff's calculus at scale; and idempotency/error-message design grounded in the MCP specification's four tool annotation hints (`readOnlyHint`/`destructiveHint`/`idempotentHint`/`openWorldHint`, their cautious defaults, and the explicit "treat as untrusted" caveat), its stateful-tool/handle-design guidance, and its two-tier protocol-error-vs-tool-execution-error taxonomy, corroborated by a fully source-read production example -- OpenCode's `edit` tool's `InvalidArgumentsError` class and its six-message error catalogue (`packages/opencode/src/tool/tool.ts`/`edit.ts`, `dev` branch) -- read side by side with Claude Code's already-documented `Edit`-tool three-gate refusal mechanics as two independently-implemented harnesses converging on the same refuse-with-a-specific-reason design |
| [System-prompt / agent-instruction design as a craft](system-prompt-design-as-craft.md) | Written 2026-08-17, updated 2026-09-01 (new §2.6 OpenCode: the model-family `provider()` dispatch to `anthropic.txt`/`beast.txt` that §2.3 already quotes is only the base tier of a per-turn-assembled prompt -- `SystemPrompt.Service`'s `environment()`/`skills()`/`mcp()` functions and `prompt.ts`'s fixed-order `Effect.all([...])` concatenation sit above it, with per-agent override keying as the most granular of the three harnesses' equivalents; separately updated 2026-09-01 with new pi material woven into §1.7/§2.5/§4.4 plus §6 Synthesis: pi's system prompt is genuinely code-assembled, `buildSystemPrompt()`, unlike e.g. OpenCode's static `anthropic.txt` prompt file), separately updated 2026-09-01 with new §6 Hermes Agent material (renumbering Synthesis to §7: source-verified from `agent/system_prompt.py`/`agent/prompt_builder.py` against Hermes' own `prompt-assembly` docs page -- three named cache-priority tiers, `stable`/`context`/`volatile`, a system prompt built at most once per session and rebuilt only on a compaction event to keep upstream prompt caches warm, and a maintainer-documented fix for exactly the date-in-prompt cache-busting problem this page's own §2.6 leaves open for OpenCode) -- sits conceptually between agent-loop.md and memory-management.md/instruction-context-budget.md/context-compression.md: those pages cover *where* instruction content loads, this page covers *how to write it well*. Grounds tool-calling-style phrasing, aggressive-language dial-back, and parallel-tool-call prompting against Anthropic's own `platform.claude.com` tool-use/prompting-best-practices docs and its `building-effective-agents`/`effective-context-engineering-for-ai-agents` engineering-blog posts (agent-computer-interface tool design, the "right altitude" framing, canonical vs. exhaustive examples); frames few-shot tool-call examples vs. prose constraints as complementary rather than competing via a fully source-read, genuinely open-source production system prompt (`packages/opencode/src/session/prompt/anthropic.txt` and the contrasting `beast.txt` for GPT-4/o1/o3 models, both `dev` branch) plus Anthropic's own Gorilla-paper-corroborated hallucinated-argument framing; covers compaction-survival phrasing by cross-reference to context-compression.md/memory-management.md's own survival tables plus Anthropic's own documented "tell the model it will be compacted" sample instruction; treats prompt-injection resistance as an authoring-side complement to (never a substitute for) permissions-and-sandboxing.md's enforcement architecture, grounded in Anthropic's own direct-vs-indirect threat-model split and documented mitigations (tool-result-only placement, provenance-naming, JSON-encoding, output-screening) cross-checked against the independently-arrived-at academic "spotlighting" defense (arXiv:2403.14720); and traces a multi-year, changelog-verified history of literal system-prompt/tool-description tuning on both Claude Code and Copilot CLI (lean-prompt model-conditional defaults, MCP description size caps, a dedicated `prompt-audit` tool, dated indirect-injection hardenings) while explicitly declining to treat community reverse-engineered system-prompt leaks (`zep-us/claude-system-prompt`, `asgeirtj/system_prompts_leaks`) as grounding for any specific claim, separately updated 01/09/2026 (new §7 DeepSeek Harness -- renumbering Synthesis to §8: the system prompt is itself Cordis-plugin-contributed, assembled per turn via `ctx.systemPrompt.section()`/`.assemble()`/`renderPrompt()` rather than a template file, with per-tool `tool:<name>` sections individually registered under an ADR-documented one-owner rule -- "every fact in the prompt has exactly one owner" -- that structurally forecloses the referential ambiguity pi's own docs can only guard against by convention, a centralized sparse-order-allocation ADR replacing collision-prone numeric literals, strict fail-loud `{{variable}}` interpolation with regression tests closing a second-order template-injection path, and a `complete: true` section flag serving as DSH's own whole-prompt-replacement lever on the same registry every partial contribution uses) |
| [Context retrieval / RAG vs. agentic search as a design space](context-retrieval-and-agentic-search.md) | Written 2026-08-17, updated 2026-09-01 (new §5 pi, book-wide "four harnesses" framing throughout: no embeddings ever, and a maintainer publicly rejected a working embeddings-search contribution on principle, GitHub issue #1255; renumbered Synthesis to §6), updated again 2026-09-01 (new §6 Hermes Agent + renumbered Synthesis to §7: source-verified `search_files` tool fuses grep/glob/`ls` behind one ripgrep-backed primitive, with its own description explicitly telling the model to use it instead of shell equivalents) -- the initial-discovery layer underneath context-compression.md (what happens once context is full) and instruction-context-budget.md (what loads eagerly): re-verifies, via fresh fetches of docs/changelogs/source/issue-trackers this session, that none of Claude Code, Copilot CLI, or OpenCode ships embeddings-based retrieval as its primary or default code-discovery strategy today, all three converging on agentic `Grep`/`Glob`/`Read`-style iterative search instead; grounds the general RAG-vs-agentic-search design space against Lewis et al.'s original RAG paper (arXiv:2005.11401), the Hugging Face agents course's Agentic RAG framing, and an Amazon Science AAAI 2026 paper on agentic keyword search vs. RAG performance (arXiv:2602.23368); documents Claude Code's own product blog naming index staleness as its explicit reason for reversing an early RAG+vector-db implementation (a secondary-sourced Boris Cherny attribution, flagged as such) while its `large-codebases` docs name a bring-your-own-RAG-via-MCP integration seam as the sanctioned extension point; precisely scopes Copilot CLI's one real embeddings-based retrieval feature (`dynamicRetrieval`) to skill/MCP instruction text rather than source code, and separately fences off GitHub's real VS-Code-scoped Copilot embedding-search feature as a different product surface (an explicit AUTHORITY OVERREACH guard); and documents OpenCode's repeatedly-requested-but-never-shipped native RAG feature alongside the third-party plugin ecosystem (OpenCodeRAG, VectorCode, a community-built Ollama+sqlite-vec+FTS5 hybrid) that has filled the gap outside its core project -- before interrogating, with tags held apart rather than blended, whether an embeddings-for-candidates/agentic-search-for-verification hybrid is a genuine technical ceiling none of the three has reached or a deliberate operational-scope choice each team declined to own; deepened 2026-08-24 with a general-RAG-background expansion (new §1.3-§1.11, explicitly separate from the per-harness findings above) sourced from eleven Hugging Face `learn/cookbook` notebooks fetched in full that session -- naive-vs-advanced RAG (single-stage FAISS retrieval vs. token-aware chunking plus ColBERTv2 cross-encoder reranking), vector-DB-specific patterns across Milvus/Elasticsearch/MongoDB Atlas, semantic caching (Chroma/FAISS, Euclidean-distance threshold), structured/grammar-constrained generation as a retrieval-adjacent reliability technique (Outlines logit bias), chunking over unstructured/mixed-format documents (the Unstructured library's typed-element partitioning), knowledge-graph RAG (Neo4j, Cypher traversal via GraphCypherQAChain as a multi-hop complement to vector similarity), a LlamaIndex-based notebook read as a deliberate negative example distinguishing agent-*framework* RAG from genuinely agentic RAG, and RAG evaluation methodology (groundedness/relevance/standalone filtering, LLM-as-judge, Answer Correctness) -- §1.3 makes an explicit, reasoned grounding-discipline call to treat `learn/cookbook` under the same shared-background-vocabulary, non-harness-authoritative treatment this page already gives Lewis et al. |
| [Advanced/novel planning and execution architectures](advanced-planning-and-execution-architectures.md) | Written 2026-08-17 -- an ORIGINAL DESIGN-SPACE SURVEY, not a research-and-report page like every other row below: re-confirms (via fresh changelog greps of both closed-source harnesses and a zero-hit `gh search code` sweep of OpenCode's `dev` branch) that none of Claude Code, Copilot CLI, or OpenCode implement tree-search/MCTS-style planning, a loop-integrated self-critique mechanism, multi-model ensembling per reasoning step, or speculative/predictive tool execution inside their primary task loop, while precisely distinguishing that negative finding from three real, shipped, but structurally different adjacent features surfaced this session (Claude Code's `/ultrareview` multi-agent-fleet code review and `/deep-research`'s claim-voting/adversarial-review, Copilot CLI's cross-model rubber-duck second opinion) and from this book's own prior findings (model-routing-and-selection.md's sequential fallback chains, streaming-and-incremental-rendering.md's speculative-JSON-parsing negative finding, fan-out.md's parallel-already-decided-calls mechanics); then surveys the actual literature grounding each of the four mechanisms (Tree of Thoughts arXiv:2305.10601, LATS arXiv:2310.04406, Tree Search for Language Model Agents arXiv:2407.01476, self-consistency arXiv:2203.11171, and process reward models arXiv:2305.20050 for tree-search/best-of-N; Reflexion arXiv:2303.11366 and multiagent debate arXiv:2305.14325 for self-critique; Mixture-of-Agents arXiv:2406.04692 for per-step ensembling; speculative decoding arXiv:2211.17192 plus the 2026 PASTE/B-PASTE/"Speculate While You Reason"/SpecBox line, arXiv:2603.18897/2604.16469/2607.25816/2607.23933, for speculative tool execution), before closing with a reasoned (BEST CURRENT UNDERSTANDING) account of why the gap plausibly exists -- cost metering, interactive-latency UX investment, and a permission architecture built around single deterministic decisions, with a read/write tool-kind safety distinction argued as the likely reason speculative execution specifically hasn't shipped -- and a from-scratch synthesis sketch reusing this book's own already-documented primitives (Dynamic workflows' script-held plan, `PostToolUse`/`tool.execute.after` hooks, fan-out's parallel-dispatch machinery, durable transcript stores) rather than inventing new infrastructure |
| [A topology of agentic systems](agent-topology.md) | Written 2026-08-17 -- GENERAL-CONCEPTS page, precedes agent-loop.md in reading order: reactive vs. deliberative agents, single-agent vs. multi-agent systems (explicitly framed as the conceptual layer orchestration.md/fan-out.md/inter-agent-messaging.md implement, not a substitute for them), tool-augmented vs. fully autonomous agents, the "LLM + tools + memory + planning" component decomposition compared against narrower (Anthropic) and broader (Hugging Face course, Lilian Weng) definitions in circulation, and where a "harness" (Claude Code's own glossary-defined term, adopted as this book's umbrella word) sits relative to a bare agentic system across all three harnesses |
| [The agent loop](agent-loop.md) | Written 2026-07-30, restyled 2026-07-30 (added a `stateDiagram-v2` for the Thought/Action/Observation cycle; no factual changes) -- GENERAL-CONCEPTS page (no harness sections, deliberately): Thought/Action/Observation, ReAct, the while-loop framing, observations appended to the prompt, stop-and-parse, JSON vs. code vs. function-calling agents |
| [Agent loop: Claude Code, Copilot CLI, and OpenCode](agent-loop-implementations.md) | Written 2026-07-30, restyled 2026-07-30 (added a `sequenceDiagram` for Claude Code's turn loop and a `flowchart` for OpenCode's step-limit prompt injection; no factual changes), updated 2026-08-24 (new §2 Copilot CLI + renumbered §3 OpenCode/§4 Comparison, closing a real per-harness-coverage gap), updated again 2026-09-01 (new §4 pi + renumbered §5 Comparison: pi's loop has NO `maxTurns`/`maxBudget`/`maxSteps` cap anywhere, unlike Claude Code or OpenCode), updated again 2026-09-01 (new §5 Hermes Agent + renumbered §6 Comparison: source-verified directly from the ~3,900-line `run_conversation` loop body in `agent/conversation_loop.py` -- a single `while` statement gating on both an iteration count and a remaining-budget check, with a genuine internal vocabulary clash over what the word "turn" itself names) -- harness-specific companion to agent-loop.md: Claude Code's Agent SDK-documented loop (turns, max_turns/max_budget_usd, compaction, parallel tool execution); Copilot CLI's own loop sourced from the adjacent Copilot SDK's `docs/features/agent-loop.md` (turn = one LLM call, `session.idle` vs. `session.task_complete`, autopilot's synthetic task_complete-nudge reinjection, a soft post-hoc `maxAiCredits` budget check) with the SDK-vs-CLI authorship distinction stated explicitly; OpenCode's documented + live-source-verified loop (primary/subagents, step-limit system-prompt injection, `packages/core/src/session/runner/max-steps.ts`); a three-way stop-enforcement/budget-shape/verifiability comparison, plus exactly where to read OpenCode's real implementation next |
| [MCP integration](mcp-integration.md) | Written 2026-07-30, restyled 2026-07-30 (added `flowchart`s for Claude Code's config-precedence resolution and Copilot CLI's discovery walk, and a `sequenceDiagram` for Claude Code's tool-search flow; no factual changes), updated 2026-08-24 (new §3 Hermes Agent + renumbered §4 Synthesis, distributed here rather than left on a deleted standalone Hermes page), updated again 2026-08-24 (new §3 OpenCode + renumbered §4 Hermes Agent/§5 Synthesis, closing a real per-harness-coverage gap this page had never filled), updated again 2026-09-01 (new §5 pi + renumbered §6 Synthesis: NO built-in MCP client at all, a stated design choice -- a third-party `pi-mcp-adapter` fills the gap) -- discovery/registration/invocation, config formats, transports, tool-calling semantics, now five harnesses: Claude Code, Copilot CLI, OpenCode's source-verified `sanitize(server)_sanitize(tool)` naming/pagination/capability-negotiation layer (`roots` enabled, `sampling`/`elicitation` commented out despite each citing a since-closed-as-completed GitHub issue -- a flagged, unresolved discrepancy) and its `.well-known/opencode` org-wide remote-default servers, and Hermes Agent's `mcp_<server>_<tool>`-namespaced registry with per-server include/exclude filtering and live runtime tool-list re-discovery |
| [Memory management](memory-management.md) | Written 2026-07-30, updated 2026-07-30 (mid-session edit/reload semantics, §1.8 + §2.5), restyled 2026-07-30 (added an architecture-overview `flowchart`, a CLAUDE.md load-order `flowchart`, a compaction-survival `stateDiagram-v2`, and a Copilot Memory `sequenceDiagram`; no factual changes), updated 2026-08-24 (new §3 Hermes Agent + renumbered §4 Synthesis, distributed here rather than left on a deleted standalone Hermes page), updated again 2026-08-24 (new §3 OpenCode + renumbered §4 Hermes Agent/§5 Synthesis, closing a real per-harness-coverage gap this page had never filled), updated again 2026-09-01 (new §4 pi + renumbered §5 Hermes Agent/§6 Synthesis: ships NO agent-authored memory tool at all, the same real absence as OpenCode, but with a source-verified `AGENTS.md`/`AGENTS.override.md`/`CLAUDE.md` same-directory precedence order), updated 01/09/2026 (new §6 DeepSeek Harness + renumbered §7 Synthesis -- ships no `AGENTS.md`/`CLAUDE.md`-equivalent project-instruction-file convention at all, its deployment persona instead a Cordis config string, and no native agent-authored memory tool either, with its own docs explicitly framing third-party memory MCP servers (Memorix, MCP Reference Memory, Engram) as default-off, non-endorsed reference integrations) -- instruction-file hierarchies, Claude Code auto memory vs. server-side Copilot Memory vs. OpenCode's source-verified finding that it ships **no native agent-authored memory tool at all** (a third-party plugin, `opencode-supermemory`, fills the gap) but re-reads its `AGENTS.md` tier-1 instructions from disk on every turn -- the strongest documented mid-session-edit-reload guarantee of any harness on this page -- plus a source-verified nearby-file auto-attach mechanism, vs. Hermes' `MEMORY.md`/`USER.md` hard-cap files with a dedicated post-turn closed-learning-loop review, `SOUL.md` identity-slot pinning, and cross-harness Context-File discovery naming `CLAUDE.md`/`AGENTS.md` directly, injection timing, whether an edited CLAUDE.md is re-read, compaction survival, session resume, now six harnesses |
| [Instruction context budget](instruction-context-budget.md) | Written 2026-07-30, restyled 2026-07-30 (added a three-tier `flowchart` comparing both harnesses' tier structure; no factual changes), updated 2026-08-24 (new §3 OpenCode + renumbered §4 Synthesis, closing a real per-harness-coverage gap this page had never filled), updated again 2026-09-01 (new §4 pi + renumbered §5 Synthesis: a genuinely new lazy-loading mechanism, provider-native deferred-schema-loading via `pi.registerTool()`/`pi.setActiveTools()`), updated again 2026-09-01 (new §5 Hermes Agent + renumbered Synthesis to §6: source-verified directly from Hermes' own `agent/system_prompt.py` rather than docs alone -- three system-prompt cache-stability tiers, `stable`/`context`/`volatile`, rebuilt at most once per session and only on a context-compression event, a cache-stability axis distinct from this page's own tier-1/2/3 lazy-load vocabulary) -- keeping the eagerly-loaded instruction tier small: why `@` imports don't help, path-scoped rules (`paths:`) vs. `applyTo:`, skills as the invoke-only tier, exclusion/trim levers, how to measure what loaded, now four harnesses: Claude Code, Copilot CLI, and OpenCode's source-verified per-turn (not per-session) tier-1 re-read, its documented absence of any path-scoped instruction tier, and its source-only nearby-instruction-file auto-attach mechanism |
| [Handoff mechanism](handoff-mechanism.md) | Written 2026-07-30, updated 2026-09-01 (new §4 pi + renumbered Synthesis to §5: no native subagent handoff, but a separate example extension, `handoff.ts`, does an LLM-generated, human-reviewed context handoff into a new session as an alternative to compaction), updated again 2026-09-01 (new §5 Hermes Agent + renumbered Synthesis to §6: `delegate_task` spawns an in-process child `AIAgent` object rather than a separate process, seeded only with the delegated goal/context and never the parent's own conversation history, with only the child's final structured summary re-entering the parent's context while the full child transcript stays in its own session row; Hermes is also the one harness whose own vocabulary literally uses the word "handoff" for a different, LLM-generated context-transfer-into-a-new-session mechanism, a distinction flagged explicitly to avoid conflating the two) -- agent-to-agent context transfer specifically (the compaction/context-survival half stays in [memory-management.md](memory-management.md) §1.7/2.4): Claude Code subagents (fresh context, resume via `SendMessage`+agent ID, forks vs. named subagents, depth/session/concurrency limits) and agent teams (peer-to-peer mailbox messaging, plan-approval/shutdown protocols); Copilot CLI custom-agent subagents, `/delegate`-to-cloud-agent, and VS Code's out-of-scope `handoffs` field checked only to rule out overreach; OpenCode's `Task` tool source-verified end to end (`session.create(parentID)`, `task_id` resume, `deriveSubagentSessionPermission`, `dev`-branch caveat) |
| [Built-in tools](built-in-tools.md) | Written 2026-07-30, updated 2026-08-17, updated again 2026-09-01 (new §4 pi + renumbered §5 Synthesis: exactly 8 built-in tools, `grep`/`find` shell out to real `ripgrep`/`fd` binaries; resolves the `pi-ai`-vs-`pi-coding-agent` naming question -- both real, correctly-scoped names), updated again 2026-09-01 (new §5 Hermes Agent + renumbered §6 Synthesis: an order-of-magnitude larger, dynamically-gated registry -- roughly 86 tools by Hermes' own reference page, a union across every platform/integration it supports rather than a fixed per-session table -- assembled from core/composite/platform/dynamic toolset layers plus capability- and workflow-level gating) -- the tool surface itself, now four harnesses: Claude Code's full `tools-reference` table (permission-required column, the `TodoWrite`->`Task*` migration and its separate, later v2.1.233 model-conditional withholding on Opus 4.8/Sonnet 5/Fable 5/Mythos 5 and later versions of those families, the three-way "Task"-prefix naming overload against `TaskOutput`/`TaskStop` and `CronCreate`/`Cron*`, cross-referenced rather than repeated against session-persistence.md §1.2's storage-mechanics gap-fill, the Edit tool's three-gate check, Bash timeout/backgrounding), Copilot CLI's permission-"kind" vocabulary (shell/write/read/url/memory/MCP-SERVER) vs. its changelog-confirmed functional tools (shell/view/web-fetch/search/task tools, `store_memory`/`vote_memory`, model-conditional `apply_patch` for Codex models, the curated-by-default GitHub MCP server), and OpenCode's documented tool list + permission model (`allow`/`ask`/`deny`, `doom_loop` guard) |
| [Context compression](context-compression.md) | Written 2026-07-30, updated 2026-09-01 (new §5 Hermes Agent: docs-verified two independently-thresholded compaction layers -- a fixed-85%-of-context-length gateway-level safety net catching sessions that grow between agent turns, layered above a primary, real-API-token-based `ContextEngine.should_compress()` 50% trigger with a pluggable compression-engine backend) -- mid-run compression mechanics distinct from the instruction-loading half ([instruction-context-budget.md](instruction-context-budget.md)) and the memory-survival half ([memory-management.md](memory-management.md) §1.7/§2.4): Claude Code's documented two-phase evict-then-summarize mechanism, Sonnet 5's ~967K auto-compact default and thrash guard; Copilot CLI's changelog-traced evolution from a 2025-10 truncation warning to 95%-trigger background checkpointed compaction to today's blocked-compaction state; OpenCode's source-verified `prune()`/`process()` pipeline (`packages/opencode/src/session/compaction.ts` + `overflow.ts`, `dev` branch), its anchored-summary Markdown template, and a flagged docs/source config-key mismatch |
| [Caching (prompt/context caching)](caching.md) | Written 2026-07-31, updated 2026-09-01 (new §4 pi + renumbered Synthesis to §5: a shared `CacheRetention` enum copy-pasted across four provider protocols, Anthropic/OpenAI/Bedrock/Google; compaction explicitly sets `cacheRetention: "none"`, the opposite of Claude Code's cache-favorable stance), updated again 2026-09-01 (new §5 Hermes Agent + renumbered Synthesis to §6: three source layers -- user docs, a developer-guide page, and `agent/prompt_caching.py` itself -- that visibly disagree with each other on the default cache TTL (the user docs' 1-hour claim vs. the code's actual 5-minute default) and on whether caching can be disabled at all, with the discrepancy stated explicitly rather than silently resolved in the harness's favor) -- server-side prefix reuse, distinct from context-compression.md (shrinking) and memory-management.md (loading): Claude Code's documented layer-ordered prefix match, the full invalidate-vs-keep-cache action lists, TTL-by-auth-path, cache scope, subagent/fork behavior, and a changelog-traced history of caching fixes (v2.1.211 Bedrock/Foundry parity, `ENABLE_PROMPT_CACHING_1H`/`FORCE_PROMPT_CACHING_5M`, `--exclude-dynamic-system-prompt-sections`); Copilot CLI's documented 10%-read-discount/24h-OpenAI-vs-1h-others TTL split and changelog-traced observability maturation (OTel GenAI cache attributes); OpenCode's source-verified `packages/llm/src/cache-policy.ts` auto-placement policy (last-tool/last-system/latest-user-message breakpoints), the shared 4-breakpoint-cap lowering for Anthropic Messages and Bedrock Converse, the OpenAI/Gemini implicit-caching no-op, and a flagged docs/source config-surface gap; plus a bounded citation of the underlying Anthropic API prompt-caching mechanism (breakpoint cap, TTL pricing, 20-block lookback) that both Claude Code and OpenCode build on |
| [Orchestration](orchestration.md) | Written 2026-07-31, updated 2026-09-01 (new §4 pi + renumbered Synthesis to §5: no built-in orchestrator/fleet primitive at all -- only the inert-by-default subagent extension, isolated at the OS-process level), updated again 2026-09-01 (new §5 Hermes Agent + renumbered Synthesis to §6: three structurally distinct coordination layers coexisting -- an in-conversation, turn-by-turn `delegate_task` that can self-nest behind an explicit `role="orchestrator"` flag, a durable out-of-process Kanban board that survives the spawning conversation's own death, and a cross-machine `a2a_orchestrate` layer for coordinating with agents outside the local Hermes process entirely -- with no other harness on this page shipping a documented equivalent to the second or third) -- who holds the plan once a task needs several agents together, distinct from handoff-mechanism.md's single-handoff mechanics: the general orchestrator/manager-agent concept (Hugging Face agents course); Claude Code's turn-by-turn model (subagents/skills/agent teams, docs' own comparison table) vs. its Dynamic workflows/Workflow tool (script-held plan, `agent()`/`pipeline()`, 16-concurrent/1,000-total-agent caps, start-order resume semantics, size guidelines, `/deep-research`); Copilot CLI's `/fleet` command and its explicitly named "orchestrator agent" (plus the separate Copilot SDK's SQL-todo/`todo_deps` Fleet-mode runtime, flagged as adjacent-surface background); OpenCode's primary-agent/subagent taxonomy (Build/Plan/General/Explore/Scout) and `permission.task` glob allow/deny (schema-omission enforcement, not runtime refusal) |
| [Fan-out (subagent dispatch)](fan-out.md) | Written 2026-07-31, updated 2026-08-20 (new §4 + renumbered §5 Synthesis), updated again 2026-08-24 (new §5 Hermes Agent + renumbered §6 Synthesis, distributed here rather than left on a deleted standalone Hermes page), updated again 2026-09-01 (new §6 pi + renumbered Synthesis to §7: NO built-in subagent mechanism -- an official but manually-symlinked example extension spawns a genuinely separate OS process per subagent) -- launch mechanics distinct from handoff-mechanism.md (what crosses the boundary) and orchestration.md (who holds the plan): Claude Code's three distinct fan-out layers (in-conversation "Run parallel research" subagents with concurrent/session/depth caps, `claude agents` background sessions with one-prompt-at-a-time dispatch and no documented hard cap, and the Workflow tool's runtime-throttled `pipeline()`); Copilot CLI's changelog-traced history (parallel tool execution's 2025-10-22 introduction and 2026-02-25 GA-mandatory removal of its opt-out flag, the subagent depth/concurrency limit's introduction/configurability/default-change, `/fleet`'s conditional dependency-based parallelism, and the experimental multiple-concurrent-sessions Sessions sidebar); OpenCode's source-verified `FiberSet`-based concurrent dispatch (`packages/opencode/src/session/llm/native-runtime.ts`, `dev` branch) showing each batched `Task` tool call forked as an independent Effect fiber with `FiberSet.awaitEmpty` as the join barrier, one layer deeper than either other harness's own documentation goes; DeepSeek Harness's docs-verified `SubagentRuntime.start()`/`.startContinuable()` one-shot-vs-continuable dispatch split, its per-provider `inheritsParentContext` descriptor decoupling context inheritance from authority (every child gets a flat scope regardless), and its qualitative, no-numeric-cap concurrency stance -- folded into the page's Synthesis table as a fourth column alongside a from-scratch reasoning-aid gap noted on the "Ralph loop"/"Ralph round" terminology; Hermes Agent's `delegate_task` tool (isolated context, restricted toolset, a *separate terminal session* per child, 3-concurrent stated default) plus its structurally unrelated "Bot Mode" pattern of named peer-specialist profiles voluntarily choosing whether to speak in a shared group chat -- a coordination shape this page's own dimension-by-dimension table cannot cleanly score, named explicitly as such rather than forced into the table's existing columns |
| [Inter-agent messaging](inter-agent-messaging.md) | Written 2026-07-31, updated 2026-09-01 (new §4 pi + renumbered Synthesis to §5: no built-in multi-agent messaging at all -- the subagent extension's own child process even closes stdin at spawn, so there is no channel to message a running subagent), updated again 2026-09-01 (new §5 Hermes Agent + renumbered Synthesis to §6: `delegate_task`'s own result delivery is a background handle returned immediately at spawn time, with the child's completion event stored durably in `state.db` and republished later as a synthetic new message onto the parent's own fresh-turn queue, rather than a synchronous return value) -- the wire format/transport once agents are already talking, distinct from handoff-mechanism.md's spawn/return mechanics and orchestration.md's who-holds-the-plan question: Claude Code's `SendMessage` tool (agent-ID-vs-name addressing with a v2.1.199 collision guard, the file-based mailbox at `~/.claude/teams/{team}/inboxes/{agent}.json` with its v2.1.207 malformed-entry validation/eviction fix, structured `shutdown_request`/`plan_approval_response` protocol messages, the permission-classifier-enforced provenance/trust boundary on received messages, and the `TeammateIdle`/`TaskCreated`/`TaskCompleted` hooks with a flagged documentation gap on their exact payload schema); Copilot CLI's one-directional `subagent.*` lifecycle event stream (SDK-documented, envelope-level `agentId`) and `/fleet`'s status-report-into-shared-todo-state coordination, with the absence of any peer-to-peer channel flagged as UNCONFIRMED-as-absent rather than proven; OpenCode's source-verified finding that there is no dedicated inter-agent message type at all -- a subagent's completion is an ordinary message row (the `Info`/`Part` schema in `packages/opencode/src/session/message.ts`/`message-v2.ts`) written into the parent session and observed through the same directory-scoped Server-Sent-Events bus (`packages/opencode/src/server/routes/instance/httpapi/handlers/event.ts`) that serves every other live update in the product |
| [Retries](retries.md) | Written 2026-07-31, updated 2026-09-01 (new §4 pi + renumbered Synthesis to §5: two-layer retry, request-level plus turn-level, and a third sibling package, `pi-agent-core`, with its own durable crash-recoverable retry state machine), updated again 2026-09-01 (new §5 Hermes Agent + renumbered Synthesis to §6: source-verified from `agent/retry_utils.py`/`agent/error_classifier.py`/`agent/conversation_loop.py` -- a single `while retry_count < max_retries` loop threading rate-limit-aware provider fallback, one-shot per-provider OAuth refresh, credential-pool rotation, and format-recovery one-shots (image shrink, thinking-signature strip, grammar fallback) ahead of jittered backoff) -- failure recovery on a single outbound model-provider request, distinct from context-compression.md (overflow handling) and caching.md (reusing a successful prefix): Claude Code's documented policy (10-attempt default, `CLAUDE_CODE_MAX_RETRIES`/`CLAUDE_CODE_RETRY_WATCHDOG`/`API_TIMEOUT_MS`, the retried-vs-not category list, the v2.1.198/2.1.199 UX and subscription-429 changes, the v2.1.98 Retry-After-as-minimum fix, the v2.1.76 compaction circuit breaker, the v2.1.110/111 fallback-retry-cap revert, `fallbackModel`); Copilot CLI's changelog-traced per-subsystem retry hardenings (0.0.389 through 1.0.66) since its own docs page turned out to be generic platform guidance, not CLI-specific, plus `continueOnAutoMode`; OpenCode's source-verified two-layer architecture -- a bounded, jittered transport retry (`packages/llm/src/route/executor.ts`) wrapped by an effectively uncapped whole-turn retry (`packages/opencode/src/session/retry.ts`'s `SessionRetry.policy`, wired via `Effect.retry` in `session/processor.ts`) -- cross-referenced against a live-fetched GitHub Issue (#17648) corroborating the unbounded-retry behavior from real usage |
| [Configuration](configuration.md) | Written 2026-07-31, updated 2026-08-24 (new §4 pi + renumbered §5 Synthesis), updated again 2026-09-01 (new §5 Hermes Agent + renumbered §6 Synthesis: a single `~/.hermes/` root splitting non-secret settings into `config.yaml` from credentials in a separate `.env` file, with a routing-aware `hermes config set` command that automatically writes each key to the right file and a `hermes config migrate` step that also discovers per-skill settings) -- the general settings/config-file system distinct from mcp-integration.md's MCP-specific config, memory-management.md/instruction-context-budget.md's instruction-file hierarchies, and built-in-tools.md's per-tool permission vocabulary: Claude Code's four-scope `settings.json` hierarchy (Managed/User/Project/Local, the permission-rules-merge exception, a flagged docs-internal inconsistency on CLI-flags-vs-Managed ordering, changelog-traced `.claude.json`->`settings.json` migration and malformed-file-resilience hardening); Copilot CLI's `~/.copilot` directory layout, its documented 6/7-link precedence chain, the bounded `.github/copilot/settings.json` repo-config key list, MDM-managed settings, and a changelog-traced structural history (`config.json`->`settings.json` renames at both repo and user scope, and its own read of Claude Code's `.claude/settings.json` as a cross-harness interop measure); OpenCode's source-verified `mergeConfigConcatArrays()` eight-source merge order, `{env:}`/`{file:}` config-value substitution, and its granular `permission` schema (allow/ask/deny, glob patterns, per-agent overrides, plus a source-only `OPENCODE_PERMISSION` env-override not found in its own docs); pi's docs-verified two-scope (global/project) model with no managed/MDM tier at all, its stated key-by-key nested-object merge (a worked `compaction.reserveTokens` example, with `defaultTools` flagged as a named wholesale-replace exception), its full settings-key-cluster and CLI-flag/env-var inventory cross-referenced out to this book's other pi sections rather than repeated, and its **Project Trust** mechanism -- a config-*loading* precondition gating the project scope itself (ask/always/never, a per-folder `trust.json` decision store, a documented non-reloading `/trust` write) that this page argues sits one step earlier in the pipeline than any of the other three harnesses' per-tool permission engines |
| [Built-in skills](built-in-skills.md) | Written 2026-07-31, updated 2026-08-24 (new §5 Hermes Agent + renumbered §6 Synthesis, distributed here rather than left on a deleted standalone Hermes page; pi's own §4 predates this index row's last rewrite) -- what ships as a *skill* by default, distinct from user-authored content, and separate from built-in-tools.md's tool surface and instruction-context-budget.md's loading/frontmatter mechanics: Claude Code's bundled-skill set present in every session (`/doctor`, `/code-review`, `/batch`, `/debug`, `/loop`, `/claude-api`, `/verify`, `/run`, `/run-skill-generator`, `/dataviz`, `/design-sync`, `/fewer-permission-prompts`) plus `disableBundledSkills`/`skillOverrides` visibility controls, vs. the separately opt-in `anthropics/skills` plugin-marketplace repo (source-available docx/pdf/pptx/xlsx powering Claude.ai directly, `skill-creator` meta-tool); Copilot CLI's changelog-confirmed v1.0.17 built-in skill, the `configure-copilot` subagent (not a skill) correctly distinguished, and its Forge-generated draft-skill pipeline reviewed via `/chronicle skills review` (the only harness-authors-its-own-skills pattern found in this book); OpenCode's skill mechanism shown to have no confirmed bundled skills at all, purely a generic `.claude/skills`-compatible extension point; pi's named, explicit `agentskills.io` conformance and first-class cross-harness skill-directory reuse; and Hermes Agent's own `agentskills.io` conformance, tool-callable three-level `skills_list()`/`skill_view()` progressive disclosure, and autonomous, unreviewed-by-default skill self-authorship via `skill_manage` |
| [The LLM API contract](llm-api-contract.md) | Written 2026-08-01, updated 2026-08-20 (new §3.4 + Synthesis/Sources additions), updated again 2026-09-01 (new §3.6 Hermes Agent: source-verified from full reads of `agent/chat_completion_helpers.py` and `pyproject.toml` -- every OpenAI-Chat-Completions-shaped provider Hermes talks to (OpenRouter, Nous Portal, GitHub Models, NVIDIA NIM, xAI, and more) is dispatched through one pinned `openai==2.24.0` SDK instance regardless of vendor, with the native Anthropic SDK an optional extra and AWS Bedrock the one transport that bypasses the OpenAI client entirely) -- the wire-level layer every other page in this book assumes already works, distinct from agent-loop.md's Thought/Action/Observation cycle built on top of it: Anthropic's Messages API (roles, the `tool_use`/`tool_result` content-block pair, the 7-value `stop_reason` enumeration, the SSE `message_start`->`content_block_delta`->`message_stop` event sequence, `input_json_delta`/`thinking_delta`/`signature_delta`, model-generation-dependent stream-interruption recovery) and OpenAI's Responses API (`tool_calls`/`call_id`, the 4-value `finish_reason` enumeration, `response.function_call_arguments.delta/done`), plus a flagged-unconfirmed sketch of classic Chat Completions; Claude Code's Agent SDK `StreamEvent`/`SDKPartialAssistantMessage` wrapping of the raw Anthropic events; Copilot CLI's changelog-confirmed "Responses API" as a real internal provider category (with the separate, adjacent Copilot SDK's source-verified `anthropic`/`openai`-completions/`openai`-responses three-way dispatch cited as consistent-but-unproven background); OpenCode's fully source-verified four-axis `Protocol`/`Endpoint`/`Auth`/`Framing` route decomposition, its shared `LLMEvent` tagged union and six-value `FinishReason` enum with both providers' `mapFinishReason()` implementations read side by side, its provider-executed-hosted-tool passthrough, and its `<system-update>`-tag fallback for providers lacking a native mid-conversation system role; DeepSeek Harness's own named **capability seam** vocabulary (§3.4, source-verified from its docs 20 August 2026) -- a Service Definition/Provider/Consumer three-role decomposition one level of abstraction above OpenCode's four axes, with `ctx.llm` as the seam governing this page's own subject and real interchangeable providers (`llm-deepseek`/`llm-pi-ai`/`llm-replay`) mounted underneath a provider-neutral agent-loop Consumer |
| [Permissions & sandboxing architecture](permissions-and-sandboxing.md) | Written 2026-08-01, updated 2026-08-20 (new §4 + renumbered §5 Synthesis), updated again 2026-08-24 (new §6 Hermes Agent + renumbered §7 Synthesis, distributed here rather than left on a deleted standalone Hermes page: an eight-layer defence-in-depth model that explicitly quotes and rejects the "trust the LLM" phrase, three command-approval modes, a hardline blocklist surviving even its own `--yolo` equivalent, container isolation that skips the approval check entirely, and a six-step gateway user-authorization chain for its multi-platform messaging surface) -- the enforcement architecture underneath configuration.md's permission-rule schema and built-in-tools.md's per-tool permission vocabulary: Claude Code's stated prompt-injection threat model, the six permission modes as an enforcement-architecture choice, the tiered approval-prompt UX (durable Bash-rule persistence vs. session-only file-edit approval, `Ctrl+E` risk explainer), the auto-mode classifier's full decision pipeline (why it exists as a prompt-fatigue mitigation, trust scope, tool-result stripping, subagent three-checkpoint handling, 3-consecutive/20-total fallback thresholds), the OS-level sandboxed-Bash tool's two independent filesystem/network layers (Seatbelt/bubblewrap, credential mask-vs-deny, `autoAllowBashIfSandboxed`) and its named escape hatches (`dangerouslyDisableSandbox`, domain fronting, `filesystem.disabled` risk, root/sudo bypass-mode refusal); Copilot CLI's documented directory-scope default and responsible-use accountability framing, its awareness-vs-execution two-layer permission model, its own OS-level `/sandbox` (General/Filesystem/Network tabs, `sandbox.allowBypass`, MDM-enforced managed sandbox floor) traced through changelog history back to at least mid-2026, and a third-party PromptArmor-reported read-only-auto-allow parsing gap (`env curl | env sh`) cross-referenced against GitHub's own quoted, unremediated-as-of-report response; OpenCode's source-verified `Permission.Service` enforcement engine (`ask`/`reply`/`list`, `Deferred`-blocked prompts wired through the same SSE event bus documented in inter-agent-messaging.md, `findLast()` last-match-wins evaluation, session-wide reject propagation) and the source-verified finding that OpenCode ships no OS-level sandbox at all -- "sandbox" in its own codebase names only an in-process JS-value data boundary (`codemode`) and a project-directory-tracking list, leaving its permission engine as the sole enforcement layer; DeepSeek Harness's own two-axis (`sandbox/mode` x `approval/policy`) permission-preset model over a Landlock+Bubblewrap/Seatbelt/Windows-ACL `ctx.sandbox` seam (filesystem effects only, self-reported `full`/`partial` enforcement) with an explicit `danger-full-access` preset that bypasses `ctx.sandbox` entirely -- source-verified 20 August 2026, folded into the page's Synthesis as a three-way "danger"-named-bypass convergence with Claude Code and Copilot CLI, plus a BEST-CURRENT-UNDERSTANDING-only reasoning aid on whether Claude Code's six permission modes factor into the same two independent axes DeepSeek's presets do |
| [Hooks and lifecycle extensibility](hooks-lifecycle-extensibility.md) | Written 2026-08-01, updated 2026-08-20 (new §4 + Synthesis/Sources additions), updated again 2026-08-24 (new §6 Hermes Agent + Synthesis/Sources additions, distributed here rather than left on a deleted standalone Hermes page: three distinct hook systems -- declarative gateway `HOOK.yaml`/`handler.py`, programmatic plugin `ctx.register_hook()`, and drop-in shell hooks -- the last of which independently reinvents Claude Code's own exit-code-2-blocks convention, plus a per-hook `fail_closed` flag and a persisted per-command consent allowlist) -- the dedicated treatment of the main mechanism harnesses expose for user-side code to sit directly inside the loop's control flow, distinct from inter-agent-messaging.md §1.6's in-passing naming of `TeammateIdle`/`TaskCreated`/`TaskCompleted` and permissions-and-sandboxing.md §1.3's in-passing note on `PreToolUse` precedence: Claude Code's full ~30-event catalogue across three cadences (session/turn/per-tool-call), the exit-code-2-only-blocks rule and per-event `hookSpecificOutput` decision shapes, five handler types (command/http/mcp_tool/prompt/agent), matcher/`if`-condition filtering, security safeguards (no controlling terminal since v2.1.139, 8-consecutive-block stop cap, workspace-trust gate on frontmatter hooks), and a changelog-traced history from the v1.0.38 origin (crediting issue #712) through v2.1.219's `DirectoryAdded`; Copilot CLI's 14 dual-camelCase/PascalCase-named events across three surfaces (CLI/cloud agent/VS Code), its event-dependent fail-open-vs-fail-closed exit-code asymmetry (`preToolUse` alone fail-closed on non-2 exits, timeouts universally fail-open), its six-tier config precedence, and its documented Claude-Code-format compatibility layer (shared tool-name mapping table, `.claude/settings.json` readable directly) traced from the v0.0.396 origin through v1.0.76's output-hardening; OpenCode's source-verified two-surface plugin model (a generic observational `event` bus vs. ~20 named, typed `(input, output)` hook functions on the `Hooks` interface in `packages/plugin/src/index.ts`, `dev` branch) with `tool.execute.before`/`tool.execute.after` confirmed wired via real `plugin.trigger()` call sites in `session/tools.ts`, a flagged `permission.ask`-vs-`permission.asked` naming/wiring discrepancy left UNCONFIRMED rather than smoothed over, and the structural absence of any dedicated blocking session-lifecycle hook; DeepSeek Harness's Cordis plugin substrate -- now this page's home for that architecture, moved here from a deleted standalone page -- covering disposer-returning `ctx.effect()`/`ctx.on()` registration as a reversible-by-construction primitive, its bundle->profile->patch boot-composition scheme, an honestly-flagged gap on a confirmed tool-execution hook catalogue, and a BEST-CURRENT-UNDERSTANDING-only reasoning aid using Cordis's disposer pattern to hypothesize about the two closed harnesses' own hook/config-reload teardown semantics |
| [Session & transcript persistence](session-persistence.md) | Written 2026-08-01, updated 2026-08-17 (new §1.2, closing a real gap `airchon-mentor` found live in a prior conversation and flagged rather than answering unresourced), updated again 2026-08-20 (new §4 + renumbered §5 Synthesis), updated again 2026-09-01 (new §6 Hermes Agent + renumbered §7 Synthesis: the only harness in this book whose entire conversation history -- across CLI, Telegram, Discord, Slack, WhatsApp, Signal, Matrix, and Teams alike -- lives in one canonical SQLite store, `~/.hermes/state.db`, WAL-mode like OpenCode's own `opencode.db`, with a `parent session ID` column supporting compression-triggered session splitting) -- single-session durability across process restarts, distinct from memory-management.md's compaction-survival-within-a-live-run and handoff-mechanism.md's agent-to-agent spawn/return: Claude Code's documented JSONL transcript (`~/.claude/projects/<project>/<session-id>.jsonl`), the `TodoWrite`-vs-`Task*` task-tracking-tool persistence split (`TodoWrite` message-stream-only today, with a docs-confirmed legacy `~/.claude/todos/` directory no longer written by current versions; the `Task*` family's own per-session `~/.claude/tasks/<session-id>/` directory, swept under the same retention as the transcript; the precise v2.1.233 model-gating boundary; a live GitHub Issue, #50656, on a task-list-UI-panel-not-re-rendering-on-resume bug and its full, checked-this-session closure history; and a three/four-way "task" naming-overload disambiguation against `TaskOutput`/`TaskStop`, `CronCreate`-scheduled tasks, and the separate "active goal" concept), what `--continue`/`--resume`/`/resume` actually restores vs. leaves behind, `/branch`/`--fork-session`'s same-process-permission-carryover-vs-separate-process distinction, `/rewind`'s file-snapshot checkpointing as a mechanism parallel to (not part of) the transcript, `cleanupPeriodDays`/`claude project purge` retention, and a changelog-traced corruption/identity/scale hardening history; Copilot CLI's documented two-store architecture (`~/.copilot/session-state/` full files vs. a derived, rebuildable `session-store.db` SQLite/FTS5 index powering `/chronicle`), its own changelog-traced hardening history, and the adjacent Copilot SDK's `session.create`/`session.resume` internal-structure detail (`plan.md`, `files/`, checkpoints) flagged as background, not CLI-confirmed; OpenCode's source-verified live architecture finding that its `dev` branch has migrated session/message storage from the community-documented flat per-message JSON layout to a SQLite database (`opencode.db`, Drizzle ORM, WAL mode, `packages/core/src/database/database.ts`) with the old format demoted to a still-used auxiliary keyed-blob store, its self-timestamping sortable `ses_`/`msg_`-prefixed ID scheme (`packages/opencode/src/id/id.ts`), its source-verified `Session.fork({sessionID, messageID?})` fork-from-an-arbitrary-earlier-message mechanics (exclusive cutoff, ID/parentID/`tail_start_id` remapping) behind the CLI's `--fork` flag, and its separate `SessionRevert.revert`/`unrevert` in-place-undo mechanism backed by a shadow git repository per project/worktree that shares object storage with the real repo; DeepSeek Harness's docs-verified turn/step session-event model with a three-domain (session/agent/capability) event split and a "model-visible means logged" invariant, its pluggable-persistence-contract finding that no backend is core-owned (a JSONL "packed chunk rows" implementation is just one such plugin), and its `fork()` mechanics (an inclusive, not exclusive, boundary sequence number; open-turn boundaries explicitly rejected rather than silently clipped; deep-cloned seed events plus `parentSession`/`seedLength`/`cwd` lineage metadata) -- folded into the page's Synthesis table as a fourth column |
| [Streaming & incremental rendering](streaming-and-incremental-rendering.md) | Written 2026-08-01, updated 2026-09-01 (new §4 pi + renumbered Synthesis to §5: renders a still-incomplete Mermaid diagram's source live mid-stream, nuancing the page's earlier "nobody speculatively parses partial structure" finding), updated again 2026-09-01 (new §5 Hermes Agent + renumbered Synthesis to §6: one of only two sections in this book sourcing Hermes' own repository directly rather than its docs -- a Python `agent/` runtime feeding a typed gateway event bus into `@hermes/ink`, a private in-repo fork of Ink vendoring the same dependency footprint upstream Ink ships, with an adaptive-batch-delay scheduler, a hard tail-cap on the live render buffer, and a forward-scanning incremental Markdown parser that freezes settled blocks and re-tokenizes only the live tail) -- the client/UI-side layer one level above llm-api-contract.md's wire-level SSE/delta contract (§1.5/§2.3/§3): buffering and reassembly discipline, not repeated at the JSON-parsing level since no harness examined here attempts to parse a genuinely incomplete tool-call JSON object mid-stream. Claude Code's changelog-traced multi-year rendering-engineering history (line-by-line text streaming's v2.1.78 origin and v2.1.81 Windows carve-out, 100ms delta-coalescing cutting streaming CPU ~37% at v2.1.191, synchronized-terminal-output flicker fixes, the `NO_FLICKER`/`/tui fullscreen` renderer pair, a WASM-to-pure-TS Yoga-layout swap, and concrete reassembly bugs fixed in v2.1.92/v2.1.94), plus an explicitly-bounded, unverified community claim about an Ink-derived custom renderer; Copilot CLI's changelog-traced v0.0.348 token-by-token-streaming origin (`--stream off`), a real streamed-chunks-leaked-into-history bug fixed the very next release, tmux synchronized-output flicker traded the *other* direction, a live GitHub Issue (#1127) confirming automatic shell-output timeline tailing, and the adjacent Copilot SDK's documented ephemeral-vs-persisted streaming-event model with its notable absence of any documented mid-stream tool-argument fragmentation; OpenCode's fully source-verified findings spanning four files/packages -- the shared republishing layer's uniform raw-string accumulation discipline (`publish-llm-event.ts`), a genuine in-codebase fork where the shared `core` reducer no-ops tool-input deltas while the TUI's and web app's own reducers each independently accumulate the raw string but gate its display until the call completes, a regex-based partial-title extraction tolerant of a still-streaming reasoning summary (`thinking.ts`), a fully source-read incremental-Markdown tokenize/freeze/heal/reuse algorithm scoped to the web/app surface only (`markdown-stream.ts`, confirmed not shared with the terminal TUI), and a fully source-read client-side typewriter-pacing algorithm (`createPacedValue`) that explicitly decouples on-screen reveal rate from network delivery cadence with its own adaptive catch-up and large-burst escape valve |
| [Model routing / selection](model-routing-and-selection.md) | Written 2026-08-01, updated 2026-08-24 (new §5 Hermes Agent + renumbered §6 Synthesis, distributed here rather than left on a deleted standalone Hermes page: named auxiliary model slots with independent per-slot fallback chains and credential-pool rotation across multiple keys for one provider, plus always-on, non-optional prompt caching; pi's own §4 -- flat, explicit, no auto-routing -- predates this index row's last rewrite too), updated 01/09/2026 (new §6 DeepSeek Harness + renumbered §7 Synthesis -- core ships no precedence stack or bundled router at all, only a named-route `GenerateOptions.provider`/`.model` resolved by the `ctx.llm` runtime plus two Cordis waterfall hooks (`agent/request`, `llm/stream`) that make an automatic router possible as an opt-in plugin rather than a core feature, concretely demonstrated by the third-party `dsh-model-router` plugin) -- the dedicated treatment of which underlying model answers a given step, distinct from fan-out.md/orchestration.md's passing mentions of a subagent's `model` property, retries.md's failure-recovery-on-an-already-chosen-model, and permissions-and-sandboxing.md §1.4's classifier-as-enforcement-architecture: Claude Code's four-tier session-model precedence stack (`/model`/`--model`/env var/settings, plus org default and `availableModels` allowlist layers), model aliases and the phase-keyed `opusplan` switch, effort levels as an adjacent axis, subagent model resolution order (`CLAUDE_CODE_SUBAGENT_MODEL` > invocation param > frontmatter > inherit) and built-in subagents' fixed cheap-model assignments, dynamic workflows' per-stage model routing, the advisor tool's capability-ranked second-opinion pairing table, availability-triggered `fallbackModel` chains vs. content-triggered Fable-5/Opus-5 safety-classifier fallback, and the auto-mode permission classifier's own Sonnet-5-default model choice; Copilot CLI's documented custom-agent-definition > `--model` > `COPILOT_MODEL` > `settings.json` > default precedence, the `/model` command's Auto option, Auto model selection's real-time-health-plus-task-complexity two-system router (GA on the CLI, named routed models GPT-5.4/GPT-5.3-Codex/Sonnet 4.6/Haiku 4.5, 10% discount), the custom-agent `model` frontmatter field's string-only type and two open GitHub Issues (#2133/#3070) documenting a real array-vs-string incompatibility with VS Code, and BYOK's four environment variables for redirecting to an entirely different provider; OpenCode's source-verified `defaultModel()` startup-resolution algorithm (config key > last-used > first-by-family-priority), per-agent `model` override with subagent-inherits-invoking-primary-agent semantics, the hidden `"title"` agent's fully source-read `getSmallModel()` family-priority resolution (with opencode-/github-copilot-/azure-/bedrock-specific overrides) as a concrete cheap-model carve-out, and a resolved-on-`dev`-branch documented-vs-source discrepancy (GitHub Issue #8609) where an older commit's unconditional fallback to OpenCode's own hosted provider is no longer present in the current source read |
| [Auth & usage accounting](auth-and-usage-accounting.md) | Written 2026-08-01, updated 2026-09-01 (new §5 Hermes Agent: the most credential-rich harness this page examines -- 100-plus named provider variables across a layered `auth.json`/`.env`/secret-manager ladder, a documented manual-vs-borrowed credential distinction (a borrowed credential is never persisted in recoverable form, only a `sha256:` fingerprint of it), and auto-discovery that reads Claude Code's own `~/.claude/.credentials.json` directly as one of five seed sources, while converging with OpenCode and pi on shipping no in-harness dollar-denominated spend ceiling at all) -- API-key/OAuth handling, token/cost tracking, and budget enforcement, distinct from caching.md §1.6/§2.4/§3's cache-hit token-field observability, agent-loop-implementations.md's brief `max_turns`/`max_budget_usd` mention, and model-routing-and-selection.md §2.4's BYOK environment-variable list (all cross-referenced, not repeated): Claude Code's six-source authentication precedence stack (gateway session > cloud provider > `ANTHROPIC_AUTH_TOKEN` > `ANTHROPIC_API_KEY` > `apiKeyHelper` > `CLAUDE_CODE_OAUTH_TOKEN` > subscription OAuth), `forceLoginMethod`/`forceLoginOrgUUID` org-pin enforcement scope, the `/usage` command's locally-computed "standard list rate" dollar figure and its changelog-traced `/cost`+`/stats`->`/usage` merge at v2.1.118, the full OpenTelemetry cost/usage metric and event catalogue (export-only, no budget metric itself), and three genuinely distinct budget-enforcement mechanisms at three different layers (Agent SDK `max_turns`/`max_budget_usd` with subagent-spend-counts-toward-the-cap, the Workflow tool's own `budget.total`/`budget.spent()`/`budget.remaining()` object -- explicitly distinguished from the unrelated, Claude-Code-unsupported Anthropic API `task_budget` beta feature -- and organization/plan-level spend limits); Copilot CLI's OAuth device/web-flow authentication with its three-tier CI environment-variable precedence (`COPILOT_GITHUB_TOKEN` > `GH_TOKEN` > `GITHUB_TOKEN`), classic-PAT rejection, its "premium request" accounting unit (agentic tool calls don't count, only prompts do; Auto model selection's 10% discount) traced through a long changelog history of `/usage` display hardening, and GitHub's own four-level billing-platform budget hierarchy ($0-default hard stop, opt-in "stop usage at limit" toggle) as the actual enforcement layer, since the CLI itself has none; OpenCode's source-verified `Auth.Service` (`auth.json` at 0600, three discriminated credential shapes, the `OPENCODE_AUTH_CONTENT` full-store override), its fully source-read models.dev-priced, per-step, `sign`-parameter-reversible SQL cost/token accumulator onto denormalized `session` table columns (cross-referenced against session-persistence.md's SQLite finding) surfaced live in the TUI's prompt-bar and subagent footers, and the source-searched finding that no user-facing spend ceiling exists anywhere in the OpenCode CLI/TUI/server itself -- the only budget-*enforcement* code in the repository belongs to OpenCode Zen's own hosted-provider spend router, not to a local session; updated 1 September 2026 (new §6 DeepSeek Harness, renumbered Synthesis to §7: the most elaborate credential design examined in this book -- two disjoint key spaces (a layered, environment-variable-shaped `CredentialRef` for API keys vs. an unlayered, plugin-owned `CredentialKey` for OAuth grants addressed as `<scope>/<id>`), a dedicated `dsh-authorization` human-sign-in seam whose own architectural decision records document it fixing a real `openai-codex` OAuth-only provider outage and a cross-tenant-billing bug (a missing credential silently falling through to an unrelated provider's own key), and a replay-exact, retry-aware `ctx.tokenMeter` token accountant -- while being the only one of the six harnesses examined that converts none of its tracked tokens into a currency figure anywhere in its own sources, and shipping no in-core budget-enforcement mechanism beyond a documented, narrow recognition (not a setting) of Claude Code's own `error_max_budget_usd` outcome when DeepSeek Harness delegates to Claude Code as a subagent) |
| [MCP supply-chain trust/vetting](mcp-supply-chain-trust.md) | Written 2026-08-17, updated 2026-09-01 (new §8 pi + renumbered Synthesis to §9: moot at the product level, since pi has no MCP client at all -- see mcp-integration.md §5), updated again 2026-09-01 (new §9 Hermes Agent + renumbered Synthesis to §10: a genuine registry-adjacent finding this page had not sourced for any other harness -- Hermes' own `optional-mcps/` catalog gates every entry behind an actual Nous-staff PR review before it is merged, a stricter bar than either the official MCP Registry's or GitHub's own identity-only vetting, though the docs still caution reading each manifest's `install.bootstrap:` commands before installing) -- the narrower "thinner gap" flagged in `HANDOFF-missing-topics.md`'s final section: not discovery/config ([MCP integration](mcp-integration.md)'s territory) or per-call enforcement ([Permissions & sandboxing architecture](permissions-and-sandboxing.md)'s territory), but whether a server is what it claims to be and stays that way after a one-time approval. Frames the question as three separable axes (identity/code-safety/continuity) and finds the same division of responsibility independently reached by the official MCP Registry's own "Trust and Security" docs (`modelcontextprotocol.io/registry/about`, namespace identity via GitHub OAuth/OIDC or Ed25519/ECDSA-signed DNS/HTTP domain proof, explicitly delegating code-safety scanning to npm/PyPI/Docker Hub and moderation-policy-confirmed to *not* delist a server merely for having known vulnerabilities), by Claude Code's own `managed-mcp.json`/`allowedMcpServers` docs (no built-in registry, Anthropic Directory review scoped to connectors only and explicitly not a security audit, a documented `serverName`-is-not-a-security-control warning), and by GitHub's own docs on the GitHub MCP Registry and custom-registry access control (explicitly "not the recommended method for restricting access," a bare "confirm that you trust the server" consent prompt, no published curation criteria found); grounds the named attack taxonomy (tool description poisoning, tool shadowing, tool-name squatting, and the rug pull specifically) in fetched Invariant Labs, ETDI-paper (arXiv:2506.01333), and ReversingLabs research, with a sequence diagram of the rug-pull timeline and an honest finding that none of the three harnesses documents re-verification of a previously-approved server's tool definitions on update; cites a fetched OX Security report on a stdio-transport command-injection design flaw (ten CVEs, Claude Code named as vulnerable via a prompt-injection-mediated variant, Anthropic's reported "by design" response) as a protocol-level, cross-client finding; and closes with a source-grounded divergence Claude Code leaves MCP servers unconstrained by its default Bash-only sandbox (an opt-in beta `@anthropic-ai/sandbox-runtime` closes the gap), Copilot CLI sandboxes locally-spawned MCP servers by an independent default-on toggle, and OpenCode provides no OS-level containment for anything, cross-referencing rather than repeating [Permissions & sandboxing architecture](permissions-and-sandboxing.md) §3's prior source-verified findings for OpenCode |
| [Evals and testing a harness](evals-and-testing-a-harness.md) | Written 2026-08-01, updated 2026-09-01 (new §5 pi + renumbered Synthesis to §6: CI-safe deterministic tests run against a synthetic in-process "faux" model provider, not recorded cassettes; a separate private `pi-evals` package exists but isn't gated in CI), updated again 2026-09-01 (new §6 Hermes Agent + renumbered Synthesis to §7: grounded in a full, 12,072-entry repository file tree plus CI workflow YAML read directly from source -- a single change-classifying `detect` job routes each PR into only the Python/frontend/Rust/installer test lanes its diff actually touches, gated behind one required `all-checks-pass` branch-protection check) -- how a from-scratch harness's own correctness (not model capability) gets validated, distinct from llm-api-contract.md's wire-level tool-call contract and streaming-and-incremental-rendering.md's client-side reassembly (both cross-referenced as the mechanism this testing infrastructure protects, not re-derived): a general four-layer pyramid (unit-level wire-format correctness -> session/integration correctness -> API/route-surface coverage -> end-to-end capability evals) grounded against the Hugging Face agents course's online/offline-evaluation and LLM-as-judge vocabulary and the GAIA benchmark; Claude Code covered via two first-party Anthropic engineering posts (representative-test-set/gate-releases-on-evals guidance with three named verification methods, and the evaluator-agent/criteria-rubric/sprint-contract pattern explicitly naming harness design as inseparable from a capability-eval score) plus `code.claude.com/docs`'s headless-mode CI scaffolding (`--bare` determinism, `--output-format json`+`--json-schema`, the `system/init` event's documented CI-gating `plugin_errors`/`mcp_server_errors` fields) and a changelog-traced regression-fix vocabulary read as bounded proxy evidence only; Copilot CLI covered via its documented non-interactive/programmatic mode (`-p`/`-s`, `--share`/`--share-gist`) and a changelog-traced scripting-scaffolding history (`--output-format json` at v0.0.422, the `PermissionRequest` hook at v1.0.16, a non-interactive background-agent race fix at v1.0.33, an operational LLM-judge auto-approval mode at v1.0.69 explicitly distinguished from an evals technique), with the absence of an Anthropic-blog equivalent flagged as a real gap; OpenCode covered end to end from real, directly-read test source -- the `@opencode-ai/http-recorder` VCR-style record/replay cassette package and its `CI=true`-hard-fails-on-missing-cassette discipline, a golden-scenario regression matrix running one shared tool-loop scenario against ~18 provider/model targets across every protocol family llm-api-contract.md documents, `ToolStream`'s deterministic unit tests asserting exact event sequences across a deliberately split-mid-string chunk boundary, event-serialization tests including a literal schema-backward-compatibility decode check, a full-session-loop integration test driven against a recorded real transcript, a dedicated DSL-driven HTTP-API route-coverage exerciser run as its own CI gate, and the CI pipeline (`test.yml`) gluing all of it together across Linux/Windows unit and Playwright e2e jobs |
